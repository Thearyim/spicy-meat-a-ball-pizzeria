//create pizzeria object
function Pizzeria() {
  this.ingredients = [];
  this.sizes = [];
}

Pizzeria.prototype.getSignaturePizza = function() {
  var signaturePizzas = [];

  return signaturePizzas;
}

function Pizza() {
  this.ingredients = [];
  this.name;
}

pizzeria.prototype.initialize = function() {
  this.sizes = [
    new Size("small"),
    new Size("medium"),
    new Size("large")
  ];
}

//function to build your own pizza
pizzeria.prototype.buildYourOwn = function() {
  this.pizzas = [
    new pizza("dough"),
  ];

  this.ingredients = [
    new Ingredient("sauce"),
    new Ingredient("toppings"),
    new Ingredient("cheese"),
  ];
}

//function returns an array of ingredients that customer pick
Pizzeria.prototype.pizzaChoice = function() {
  var pizzaChoice = [];
  var buildYourOwn = [];
  var customMade = [];

if (customMadePizza = true) {
  getPizza = customMadePizza;
}
else {
  getPizza = buildYourOwn;
}
